/* Flexbox generator (placeholder: state only; core and DOM layers come in later tickets). */
(function () {
  'use strict';

  window.CssBlocks.register('flexbox', {
    state: { itemCount: 3 },
  });
})();
